# 手机接着搞 · GitHub 上云部署（接手指南）

> 本仓库已在本机 `git init` + 首提交完成，**零明文密钥**，仅差「推上 GitHub」这一步。
> 你没 GitHub 账号，所以这步由你在手机上开个免费账号、再把账号信息回给我（或自己推）即可。

---

## 已经帮你做好的（不用再动）
- ✅ 三站脚本原样就位：`pt_sign/pt_sign.py` `wnflb-checkin/wnflb_checkin.py` `checkin_merged/daily_checkin_merged.py`
- ✅ 定时工作流：`.github/workflows/daily-checkin.yml` → 北京 **07:00** 自动跑 + 支持手动触发
- ✅ 密钥外置：6 个 GitHub Secrets，`prepare_env.py` 运行时注入，代码里无密码
- ✅ 本地真实签到校验通过：三站全部成功（EXIT=0）
- ✅ 本地已提交（commit `8d6a9dc`，9 文件，无密钥）

---

## 你只需做 3 步

### 第 1 步：手机注册 GitHub（2 分钟，免费）
- 手机浏览器开 `https://github.com` 或装 **GitHub** App
- 点 Sign up：填**邮箱**（收验证邮件）、设**密码**、起一个**用户名**
- 验证：GitHub 会发**短信验证码**到你手机 `135****9525`，填进去
- 完成后记住你的 **用户名** 和 **邮箱**

### 第 2 步：把账号交给我，或全部自己推
**方案 A（推荐，最省心）**：回到电脑这边的对话，把下面两样发我：
- 你的 GitHub **用户名**
- 一个 **有 repo 权限的 Token**（GitHub 网页 → Settings → Developer settings → Personal access tokens → 勾 `repo` → 生成）
→ 我直接帮你建**私有**仓库、push、配好 6 个 Secrets、开启每天 07:00 定时。你不用敲任何命令。

**方案 B（自己推，电脑上跑）**：在仓库目录执行（见 `README.md`）：
```bash
git branch -M main
git remote add origin git@github.com:<你的用户名>/forum-checkin.git
git push -u origin main
```
然后在仓库 `Settings → Secrets and variables → Actions` 加下面 6 个 Secrets。

### 第 3 步：手动验证一次
GitHub 网页/App → 进你的仓库 → **Actions** → 点 **Run workflow** 跑一次 → 看到企业微信收到签到推送即成功。之后每天北京 07:00 自动跑，电脑关机也不影响。

---

## 6 个 Secrets 填什么（取值都来自你本机现有配置文件）
| Secret 名 | 取值来源 |
|---|---|
| `PTTIME_COOKIE` | `D:\workbuddy\...\pt_sign\config.json` → `sites[0].cookie` |
| `PTSKIT_COOKIE` | `D:\workbuddy\...\pt_sign\config.json` → `sites[1].cookie` |
| `WNFLB_USERNAME` | `D:\workbuddy\...\wnflb-checkin\secrets.json` → `username` |
| `WNFLB_PASSWORD` | `D:\workbuddy\...\wnflb-checkin\secrets.json` → `password` |
| `WECOM_WEBHOOK` | `D:\workbuddy\...\pt_sign\config.json` → `push.wecom_webhook` |
| `SERVERCHAN_KEY` | `D:\workbuddy\...\pt_sign\config.json` → `push.serverchan_key` |

> 选方案 A 的话，这些我会自动从你本机配置读走，你不用手动抄。

---

## 两点提醒
- **PT 站 cookie 会过期**：云端无持久 cookie，失效时 Actions 日志报"cookie 失效"，届时更新对应 Secret 即可。
- **福利吧**：云端每次用账号密码 + ddddocr 自动登录（不依赖本地 cookie 缓存），最稳。
